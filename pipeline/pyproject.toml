[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "interviewlens"
version = "0.1.0"
description = "Real-time multimodal interview preparation and feedback pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
